# Docker Compose — Mạng Hyperledger Fabric cơ bản
#
# Gồm 3 services:
#   - orderer.example.com  (Raft orderer)
#   - peer0.org1.example.com
#   - peer0.org2.example.com
#
# Chạy từ thư mục gốc của project:
#   docker compose -f configs/compose/compose-test-net.yaml up -d
#
# Yêu cầu: thư mục organizations/ đã được tạo bởi cryptogen (Bước 2)

version: '3.7'

volumes:
  orderer.example.com:
  peer0.org1.example.com:
  peer0.org2.example.com:

networks:
  fabric_test:
    name: fabric_test

services:

  ##############################################################################
  # Orderer
  ##############################################################################
  orderer.example.com:
    container_name: orderer.example.com
    image: hyperledger/fabric-orderer:latest
    labels:
      service: hyperledger-fabric
    environment:
      - FABRIC_LOGGING_SPEC=INFO          # Log level: DEBUG | INFO | WARN | ERROR
      - ORDERER_GENERAL_LISTENADDRESS=0.0.0.0
      - ORDERER_GENERAL_LISTENPORT=7050

      # MSP: identity của orderer
      - ORDERER_GENERAL_LOCALMSPID=OrdererMSP
      - ORDERER_GENERAL_LOCALMSPDIR=/var/hyperledger/orderer/msp

      # TLS: mã hóa kết nối
      - ORDERER_GENERAL_TLS_ENABLED=true
      - ORDERER_GENERAL_TLS_PRIVATEKEY=/var/hyperledger/orderer/tls/server.key
      - ORDERER_GENERAL_TLS_CERTIFICATE=/var/hyperledger/orderer/tls/server.crt
      - ORDERER_GENERAL_TLS_ROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]

      # Raft: không dùng system channel (cách mới từ Fabric 2.3+)
      - ORDERER_GENERAL_BOOTSTRAPMETHOD=none

      # Channel Participation API: cho phép tạo channel qua osnadmin
      - ORDERER_CHANNELPARTICIPATION_ENABLED=true

      # Admin API: port để gọi osnadmin channel join/list
      - ORDERER_ADMIN_TLS_ENABLED=true
      - ORDERER_ADMIN_TLS_CERTIFICATE=/var/hyperledger/orderer/tls/server.crt
      - ORDERER_ADMIN_TLS_PRIVATEKEY=/var/hyperledger/orderer/tls/server.key
      - ORDERER_ADMIN_TLS_ROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]
      - ORDERER_ADMIN_TLS_CLIENTROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]
      - ORDERER_ADMIN_LISTENADDRESS=0.0.0.0:7053

      # Operations: health check và metrics
      - ORDERER_OPERATIONS_LISTENADDRESS=orderer.example.com:9443
      - ORDERER_METRICS_PROVIDER=prometheus

    working_dir: /root
    command: orderer

    volumes:
      # MSP certs (từ Bước 2 - cryptogen)
      - ../../organizations/ordererOrganizations/example.com/orderers/orderer.example.com/msp:/var/hyperledger/orderer/msp
      # TLS certs
      - ../../organizations/ordererOrganizations/example.com/orderers/orderer.example.com/tls:/var/hyperledger/orderer/tls
      # Persistent data volume
      - orderer.example.com:/var/hyperledger/production/orderer

    ports:
      - 7050:7050   # Orderer gRPC endpoint (nhận transactions)
      - 7053:7053   # Admin API (osnadmin)
      - 9443:9443   # Operations (health check, Prometheus metrics)

    networks:
      - fabric_test

  ##############################################################################
  # Peer0 - Org1
  ##############################################################################
  peer0.org1.example.com:
    container_name: peer0.org1.example.com
    image: hyperledger/fabric-peer:latest
    labels:
      service: hyperledger-fabric
    environment:
      - FABRIC_LOGGING_SPEC=INFO
      - FABRIC_CFG_PATH=/etc/hyperledger/peercfg   # Vị trí core.yaml

      # Docker: cho phép peer tạo chaincode containers
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_test  # Chaincode container join cùng network

      # Peer identity
      - CORE_PEER_ID=peer0.org1.example.com
      - CORE_PEER_ADDRESS=peer0.org1.example.com:7051
      - CORE_PEER_LISTENADDRESS=0.0.0.0:7051
      - CORE_PEER_CHAINCODEADDRESS=peer0.org1.example.com:7052
      - CORE_PEER_CHAINCODELISTENADDRESS=0.0.0.0:7052

      # MSP
      - CORE_PEER_LOCALMSPID=Org1MSP
      - CORE_PEER_MSPCONFIGPATH=/etc/hyperledger/fabric/msp

      # TLS
      - CORE_PEER_TLS_ENABLED=true
      - CORE_PEER_TLS_CERT_FILE=/etc/hyperledger/fabric/tls/server.crt
      - CORE_PEER_TLS_KEY_FILE=/etc/hyperledger/fabric/tls/server.key
      - CORE_PEER_TLS_ROOTCERT_FILE=/etc/hyperledger/fabric/tls/ca.crt

      # Gossip: peer-to-peer communication để sync ledger
      - CORE_PEER_GOSSIP_USELEADERELECTION=true
      - CORE_PEER_GOSSIP_ORGLEADER=false
      - CORE_PEER_GOSSIP_EXTERNALENDPOINT=peer0.org1.example.com:7051  # Endpoint cho peers từ org khác
      - CORE_PEER_GOSSIP_BOOTSTRAP=peer0.org1.example.com:7051         # Peer để bootstrap gossip

      # Operations
      - CORE_OPERATIONS_LISTENADDRESS=peer0.org1.example.com:9444
      - CORE_METRICS_PROVIDER=prometheus

    volumes:
      # MSP certs
      - ../../organizations/peerOrganizations/org1.example.com/peers/peer0.org1.example.com/msp:/etc/hyperledger/fabric/msp
      # TLS certs
      - ../../organizations/peerOrganizations/org1.example.com/peers/peer0.org1.example.com/tls:/etc/hyperledger/fabric/tls
      # Docker socket: cho phép peer tạo chaincode Docker containers
      - /var/run/docker.sock:/host/var/run/docker.sock
      # core.yaml: config mặc định cho peer (từ fabric-samples/config/)
      - ../../fabric-samples/config:/etc/hyperledger/peercfg
      # Persistent data
      - peer0.org1.example.com:/var/hyperledger/production

    working_dir: /root

    command: peer node start

    ports:
      - 7051:7051   # Peer gRPC endpoint
      - 9444:9444   # Operations

    networks:
      - fabric_test

  ##############################################################################
  # Peer0 - Org2
  ##############################################################################
  peer0.org2.example.com:
    container_name: peer0.org2.example.com
    image: hyperledger/fabric-peer:latest
    labels:
      service: hyperledger-fabric
    environment:
      - FABRIC_LOGGING_SPEC=INFO
      - FABRIC_CFG_PATH=/etc/hyperledger/peercfg

      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_test

      - CORE_PEER_ID=peer0.org2.example.com
      - CORE_PEER_ADDRESS=peer0.org2.example.com:9051
      - CORE_PEER_LISTENADDRESS=0.0.0.0:9051
      - CORE_PEER_CHAINCODEADDRESS=peer0.org2.example.com:9052
      - CORE_PEER_CHAINCODELISTENADDRESS=0.0.0.0:9052

      - CORE_PEER_LOCALMSPID=Org2MSP
      - CORE_PEER_MSPCONFIGPATH=/etc/hyperledger/fabric/msp

      - CORE_PEER_TLS_ENABLED=true
      - CORE_PEER_TLS_CERT_FILE=/etc/hyperledger/fabric/tls/server.crt
      - CORE_PEER_TLS_KEY_FILE=/etc/hyperledger/fabric/tls/server.key
      - CORE_PEER_TLS_ROOTCERT_FILE=/etc/hyperledger/fabric/tls/ca.crt

      - CORE_PEER_GOSSIP_USELEADERELECTION=true
      - CORE_PEER_GOSSIP_ORGLEADER=false
      - CORE_PEER_GOSSIP_EXTERNALENDPOINT=peer0.org2.example.com:9051
      - CORE_PEER_GOSSIP_BOOTSTRAP=peer0.org2.example.com:9051

      - CORE_OPERATIONS_LISTENADDRESS=peer0.org2.example.com:9445
      - CORE_METRICS_PROVIDER=prometheus

    volumes:
      - ../../organizations/peerOrganizations/org2.example.com/peers/peer0.org2.example.com/msp:/etc/hyperledger/fabric/msp
      - ../../organizations/peerOrganizations/org2.example.com/peers/peer0.org2.example.com/tls:/etc/hyperledger/fabric/tls
      - /var/run/docker.sock:/host/var/run/docker.sock
      - ../../fabric-samples/config:/etc/hyperledger/peercfg
      - peer0.org2.example.com:/var/hyperledger/production

    working_dir: /root
    command: peer node start

    ports:
      - 9051:9051   # Peer gRPC endpoint
      - 9445:9445   # Operations

    networks:
      - fabric_test
