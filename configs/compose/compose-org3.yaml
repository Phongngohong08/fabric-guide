# Docker Compose — Org3 peer
#
# File này chạy peer0.org3 và kết nối vào network fabric_test đang chạy.
# Yêu cầu: mạng cơ bản (compose-test-net.yaml) đã được khởi động trước.
#
# Chạy từ thư mục gốc project:
#   docker compose -f configs/compose/compose-org3.yaml up -d

volumes:
  peer0.org3.example.com:

networks:
  fabric_test:
    name: fabric_test
    external: true    # Kết nối vào network đang có, không tạo mới

services:

  peer0.org3.example.com:
    container_name: peer0.org3.example.com
    image: hyperledger/fabric-peer:latest
    labels:
      service: hyperledger-fabric
    environment:
      - FABRIC_LOGGING_SPEC=INFO
      - FABRIC_CFG_PATH=/etc/hyperledger/peercfg

      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=fabric_test

      - CORE_PEER_ID=peer0.org3.example.com
      - CORE_PEER_ADDRESS=peer0.org3.example.com:11051
      - CORE_PEER_LISTENADDRESS=0.0.0.0:11051
      - CORE_PEER_CHAINCODEADDRESS=peer0.org3.example.com:11052
      - CORE_PEER_CHAINCODELISTENADDRESS=0.0.0.0:11052

      - CORE_PEER_LOCALMSPID=Org3MSP
      - CORE_PEER_MSPCONFIGPATH=/etc/hyperledger/fabric/msp

      - CORE_PEER_TLS_ENABLED=true
      - CORE_PEER_TLS_CERT_FILE=/etc/hyperledger/fabric/tls/server.crt
      - CORE_PEER_TLS_KEY_FILE=/etc/hyperledger/fabric/tls/server.key
      - CORE_PEER_TLS_ROOTCERT_FILE=/etc/hyperledger/fabric/tls/ca.crt

      - CORE_PEER_GOSSIP_USELEADERELECTION=true
      - CORE_PEER_GOSSIP_ORGLEADER=false
      - CORE_PEER_GOSSIP_EXTERNALENDPOINT=peer0.org3.example.com:11051
      - CORE_PEER_GOSSIP_BOOTSTRAP=peer0.org3.example.com:11051

      - CORE_OPERATIONS_LISTENADDRESS=peer0.org3.example.com:11444
      - CORE_METRICS_PROVIDER=prometheus

    volumes:
      - ../../organizations/peerOrganizations/org3.example.com/peers/peer0.org3.example.com/msp:/etc/hyperledger/fabric/msp
      - ../../organizations/peerOrganizations/org3.example.com/peers/peer0.org3.example.com/tls:/etc/hyperledger/fabric/tls
      - /var/run/docker.sock:/host/var/run/docker.sock
      - ../node-config:/etc/hyperledger/peercfg
      - peer0.org3.example.com:/var/hyperledger/production

    working_dir: /root
    command: peer node start

    ports:
      - 11051:11051   # Peer gRPC endpoint
      - 11444:11444   # Operations

    networks:
      - fabric_test
